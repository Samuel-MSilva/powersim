<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8" />
  <title><%= dispositivo ? 'Editar Dispositivo' : 'Novo Dispositivo' %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

  <div class="container py-4">
    <h1 class="mb-4">
      <%= dispositivo ? '✏️ Editar Dispositivo' : '➕ Novo Dispositivo' %>
    </h1>

    <form action="<%= dispositivo ? `/dispositivos/${dispositivo.id}/editar` : '/dispositivos/novo' %>" method="POST" class="bg-white p-4 rounded shadow-sm">
      <div class="mb-3">
        <label for="nome" class="form-label">Nome</label>
        <input type="text" class="form-control" id="nome" name="nome" value="<%= dispositivo?.nome || '' %>" required>
      </div>

      <div class="mb-3">
        <label for="potencia" class="form-label">Potência (W)</label>
        <input type="number" class="form-control" id="potencia" name="potencia" value="<%= dispositivo?.potencia || '' %>" required>
      </div>

      <div class="mb-3">
        <label for="horas_por_dia" class="form-label">Horas por dia</label>
        <input type="number" step="0.1" class="form-control" id="horas_por_dia" name="horas_por_dia" value="<%= dispositivo?.horas_por_dia || '' %>" required>
      </div>

      <button type="submit" class="btn btn-primary">
        <%= dispositivo ? 'Salvar Alterações' : 'Cadastrar' %>
      </button>
      <a href="/dispositivos" class="btn btn-secondary ms-2">Cancelar</a>
    </form>
  </div>

</body>
</html>
