<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8" />
  <title>Dispositivos - PowerSim</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light text-dark">

  <div class="container py-4">
    <h1 class="mb-3">🔌 Dispositivos Cadastrados</h1>
    <p class="text-muted">💰 Tarifa atual: <strong>R$ <%= tarifa %>/kWh</strong></p>

    <table class="table table-bordered table-hover bg-white shadow-sm">
      <thead class="table-secondary">
        <tr>
          <th>Nome</th>
          <th>Potência (W)</th>
          <th>Horas/Dia</th>
          <th>Consumo/Dia (kWh)</th>
          <th>Consumo/Mês (kWh)</th>
          <th>Custo/Dia (R$)</th>
          <th>Custo/Mês (R$)</th>
          <th>Ações</th>
        </tr>
      </thead>
      <tbody>
        <% dispositivos.forEach(d => { %>
          <tr>
            <td><%= d.nome %></td>
            <td><%= d.potencia %></td>
            <td><%= d.horas_por_dia %></td>
            <td><%= d.consumoDiaKWh %></td>
            <td><%= d.consumoMesKWh %></td>
            <td>R$ <%= d.custoDia %></td>
            <td>R$ <%= d.custoMes %></td>
            <td>
              <div class="d-flex gap-2">
                <form action="/dispositivos/<%= d.id %>/delete" method="POST" onsubmit="return confirm('Deseja excluir?')">
                  <button type="submit" class="btn btn-sm btn-danger">🗑</button>
                </form>
                <a href="/dispositivos/<%= d.id %>/editar" class="btn btn-sm btn-primary">✏️</a>
              </div>
            </td>
          </tr>
        <% }) %>
      </tbody>
    </table>

    <div class="mt-4">
      <a href="/" class="btn btn-outline-secondary">⬅ Página Inicial</a>
      <a href="/dispositivos/novo" class="btn btn-success">+ Novo Dispositivo</a>
      <a href="/dispositivos/exportar" class="btn btn-outline-info">⬇ Exportar CSV</a>
      <a href="/dispositivos/config" class="btn btn-warning">⚙ Configurar Tarifa</a>
      <a href="/dispositivos/resumo" class="btn btn-outline-primary">📊 Ver Resumo</a>
    </div>
  </div>

</body>
</html>
